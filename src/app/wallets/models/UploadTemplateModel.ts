export interface UploadTemplateModel {
  beneficiaryId: string;
  contact: string;
  idNum: string;
  bankCode: string;
  branchNumber: string;
  accountNumber: string;
  amount: string;
  comment:string;
}
