import { TransactionDateRangeDirective } from './transaction-date-range.directive';

describe('TransactionDateRangeDirective', () => {
  it('should create an instance', () => {
    const directive = new TransactionDateRangeDirective();
    expect(directive).toBeTruthy();
  });
});
