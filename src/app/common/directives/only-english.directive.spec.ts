import { OnlyEnglishDirective } from './only-english.directive';

describe('OnlyEnglishDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyEnglishDirective();
    expect(directive).toBeTruthy();
  });
});
