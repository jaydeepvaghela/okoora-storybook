import { TooltipMenuDirective } from './tooltip-menu.directive';

describe('TooltipMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new TooltipMenuDirective();
    expect(directive).toBeTruthy();
  });
});
