import { CurrencyBoxFocusDirective } from './currency-box-focus.directive';

describe('CurrencyBoxFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new CurrencyBoxFocusDirective();
    expect(directive).toBeTruthy();
  });
});
