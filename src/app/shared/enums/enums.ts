export enum ErpConnectionStatus {
  Connected = 1,
  Disconnected = 2,
  Connection = 3,
}

export enum ErpConnectionType {
    ERPConnected = 'ERP Connected',
    ERPDisconnected = 'ERP Disconnected',
    ERPConnection = 'ERP Connection'
}

